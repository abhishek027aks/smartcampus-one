# SmartCampus One — GitHub Branding

Place `logo.svg` and `banner.svg` in `assets/` beside `README.md`.
The README already references these files using relative GitHub paths.

No secrets, production `.env`, student photos, API keys, or database credentials
should be committed to the repository.
